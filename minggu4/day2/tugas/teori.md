1. tidak bisa di submit jika kosong.
2. kasih  `pattern=".+@.+"`
3. `min` untuk angka/date, `minlength` untuk teks.
4. gunakan `event.preventDefault()`
5. HTML = otomatis, JS = manual dengan logika custom.
---
